# Agent startup
start_here = "AGENTS.md"
ignore_files = ["AGENT_START_HERE.md", "AGENT_HANDOVER_MESSAGE.md"]
# IMPORTANT:
# All agents must read AGENTS.md and follow the current deployment rules.
# The owner currently allows ANY agent to deploy to STAGING, but LIVE deploys are only allowed
# when the owner clearly says “deploy live”.
# Google sign-in settings live in Google Cloud project:
# "My First Project" (project id: red-charger-462015-u3).
