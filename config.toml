# Agent startup
start_here = "AGENTS.md"
ignore_files = ["AGENT_START_HERE.md", "AGENT_HANDOVER_MESSAGE.md"]
# IMPORTANT:
# Read AGENTS.md first. The owner prefers LIVE-first deploys during early development.
# OPENAI KEY WARNING:
# Do not export, copy, print, or use OPENAI_API_KEY from any agent script or terminal command.
# The key is only for the Helfi app UI/server while testing actual app features.
# Google sign-in settings live in Google Cloud project:
# "My First Project" (project id: red-charger-462015-u3).
