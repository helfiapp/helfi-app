export const APP_NAME = 'Helfi'

// The phone app will talk to the same backend as the website.
// Default to LIVE, but you can override it when needed.
export const API_BASE_URL = process.env.EXPO_PUBLIC_API_BASE_URL || 'https://helfi.ai'
